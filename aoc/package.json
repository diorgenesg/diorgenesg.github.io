{
  "name": "aoc",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "homepage": "https://diorgenesg.github.io/aoc",
  "scripts": {
    "postinstall": "yarn elm-tooling install",
    "dev": "yarn vite",
    "prebuild": "yarn elm-tooling install",
    "build": "yarn vite build",
    "serve": "yarn vite preview",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "elm-tooling": "^1.10.0",
    "gh-pages": "^4.0.0",
    "vite": "^3.2.4",
    "vite-plugin-elm": "^2.7.2"
  },
  "dependencies": {
    "elm-debug-transformer": "^1.1.2",
    "html2canvas": "^1.4.1"
  }
}
